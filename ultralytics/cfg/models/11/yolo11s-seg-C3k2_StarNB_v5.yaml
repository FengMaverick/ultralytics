# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11-seg instance segmentation model with StarNet Blocks
# Customized for StarBlock integration

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n-seg.yaml' will call yolo11-seg.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] 
  s: [0.50, 0.50, 1024] 
  m: [0.50, 1.00, 512] 
  l: [1.00, 1.00, 512] 
  x: [1.00, 1.50, 512] 

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]] # 2-P3/8 (浅层保持原样)
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  
  # 【浅层继续保持原样】因为通道少，StarNet优势不明显
  - [-1, 2, C3k2, [512, False, 0.25]] # 4-P3/8 
  
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  
  # 【修改点 1】Backbone P4
  # 原文是 [512, True]，这里必须改为 False 以触发 StarNetBlock
  - [-1, 2, C3k2_StarNB, [512, False]] # 6 
  
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  
  # 【修改点 2】Backbone P5
  # 原文是 [1024, True]，改为 False
  - [-1, 2, C3k2_StarNB, [1024, False]] # 8
  
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, False]] # 13 (保持原样，通道较少)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 16 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  
  # 【修改点 3】Head P4
  # 这里原本就是 False，只需要改类名
  - [-1, 2, C3k2_StarNB, [512, False]] # 19 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  
  # 【修改点 4】Head P5
  # 原文是 [1024, True]，这里必须改为 False
  - [-1, 2, C3k2_StarNB, [1024, False]] # 22 (P5/32-large)

  - [[16, 19, 22], 1, Segment, [nc, 32, 256]] # Detect(P3, P4, P5)